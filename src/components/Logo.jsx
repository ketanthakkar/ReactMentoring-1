import React from 'react';
import styled from 'styled-components';

const LogoWrapper = styled.span`
    color: #f55263;
    font-size: 20px;
`;

const Logo = () => (
    <LogoWrapper>netflixroullete</LogoWrapper>
);

export default Logo;
